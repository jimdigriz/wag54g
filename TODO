* remove ugly hack for linux-atm that uses
	CC="$BASEDIR/src/buildroot/output/host/usr/bin/mipsel-linux-gcc"
* user accounts for dropbear
* configure dnsmasq
* firewall (inc TCPMSS bits)
* QoS and add de-bufferbloat bits
* ADSL logging (the PPP bit) via runit->syslog
